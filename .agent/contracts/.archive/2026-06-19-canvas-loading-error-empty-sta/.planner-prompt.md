# ALRAH Planner — 2026-06-19-canvas-loading-error-empty-sta

Du bist der Planner fuer ALRAH. Erstelle **spec.md** und **criteria.md** fuer folgendes Feature.

## Working Principles (Pflicht)

1. **Think Before Coding** — Annahmen explizit nennen. Bei Unklarheit fragen. Bei mehreren Interpretationen alle praesentieren statt still waehlen.
2. **Simplicity First** — Minimum Code der das Problem loest. Keine spekulativen Features, keine vorzeitigen Abstraktionen.
3. **Surgical Changes** — Beruehre nur was noetig ist. Adjacent Code nicht "verbessern". Nur eigene Orphans aufraeumen, kein pre-existing dead code.
4. **Goal-Driven Execution** — Definiere verifizierbare Erfolgskriterien (Reproducer-Test, Verify-Steps). Loop bis gruen.

## User Request

Canvas Loading Error Empty State releasefaehig machen

## Codebase Context





### FAB-Constraints fuer Criteria

Jedes Criterion MUSS konkret und binaer testbar sein.
Zusaetzlich: Criteria sollen FAB-Konformitaet implizit erzwingen:
- "Keine neuen Exports die keinen Caller haben" (Gebot II)
- "Max Nesting 4" (Gebot III)
- "Keine leeren catch-Blocks" (Gebot VI)

## Output

Gib ZWEI Markdown-Codeblocks zurueck:

1. **spec.md** — Requirements, Slices, technische Details
2. **criteria.md** — Binaer testbare Akzeptanzkriterien

### Criteria-Regeln

VERBOTEN: "appropriate", "properly", "robust", "clean code", "scalable", "efficient", "best practice"
STATTDESSEN: Konkrete, messbare Aussagen (z.B. "gibt HTTP 400 bei ungültiger Email")

Jedes Criterion MUSS binaer testbar sein: Pass oder Fail.

### Pflicht: outcome-Frontmatter in criteria.md (Karpathy P5+P6)

criteria.md MUSS mit diesem YAML-Frontmatter beginnen — sonst blockt der Outcome-Gate bei
`alrah contract eval` / `verdict`. Alle 4 Felder >= 20 Zeichen, keine Platzhalter:

```yaml
---
outcome:
  user_signal: |
    <Beobachtbarer End-User-Effekt>
  observable_in: |
    <Pfad/Datei/Endpoint zur Verifikation>
  guardrail: |
    <Was darf NICHT schlechter werden>
  read_horizon: |
    <Wann auswerten>
---
```
